<h1>Routes in <%= @area.name %></h1>

<ul>
  <% @lines.each do |line| %>
    <li>
      <%= link_to line.name, line_path(line) %>
      <%= line.grade %>
      <%= line.category %>
    </li>
  <% end %>
</ul>

<%= link_to "Back to all areas", areas_path, class: "btn-main" %>
